{
  "name": "all-in-one-webhook-tools",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview" 
    // TIDAK ADA KOMA di sini
  }, 
  // TIDAK ADA KOMA di sini jika ini properti terakhir sebelum "devDependencies"
  "devDependencies": {
    "vite": "^5.0.0" 
    // TIDAK ADA KOMA di sini
  }
}
